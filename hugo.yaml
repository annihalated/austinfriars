# Minimal Hugo RSS config - add to hugo.yaml

title: "Nihal Sahu"
params:
  description: "Research Fellow at Vidhi working on making Indian law simpler, clearer, and more effective"

baseURL: "/"
languageCode: "en-us"

rssLimit: 40
staticDir: "static"
canonifyURLs: true

taxonomies:
  category: 'categories'
  tag: 'tags'
  
params:

  description: "A better starting point for building Hugo sites."

markup:
  defaultMarkdownHandler: goldmark
  goldmark: 
    renderer:
      unsafe: true